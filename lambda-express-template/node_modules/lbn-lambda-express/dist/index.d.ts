import { Express } from "express";
import { LambdaEvent } from "./services/LambdaMiddlewareMapper";
/**
 * @Method: Returns the lambda handler with the express app setup
 */
export declare const createLambdaHandler: (app: Express) => (event: LambdaEvent) => Promise<any>;
